# For archives only. This file is not currently in use. 

# docker compose -f ./redis-compose.yml up

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  replica:
    image: redis:7
    command: redis-server --replicaof redis 6379
    ports:
      - "6380:6379"